<% [:toastr, :notice, :alert].each do |type| %>
  <% next unless flash[type] %>
  <script type="text/javascript">
    <% Array(flash[type]).each do |msg| %>
      Materialize.toast("<%= msg %>", 4000);
    <% end %>
  </script>
<% end %>
